Test can be used for checking various descriptors requests.
After run, devices is configured with simple descriptor.
ZC starts network, and other devices can join to ZC and sends requests to coordinator.
With ZR role, device starts and joins to existing network and also can be used for requesting descriptors.